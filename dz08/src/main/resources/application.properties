# H2
#spring.h2.console.enabled=true
#spring.h2.console.path=/h2
# Datasource
#spring.datasource.url=jdbc:h2:file:~/test.db
#spring.datasource.username=sa
#spring.datasource.password=
#spring.datasource.driver-class-name=org.h2.Driver
#spring.jpa.hibernate.ddl-auto=update

#Basic Spring Boot Config for Oracle
spring.datasource.url= jdbc:oracle:thin:@//VM-DIT-LVOV.abr.loc:1521/XE
spring.datasource.username=BOOKLIB
spring.datasource.password=BOOKLIB
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# this will drop entity table when application exits and re-create them empty when app starts:
# spring.jpa.hibernate.ddl-auto=create-drop

# this will create entity table if not exists, but table will not be re-created after app restarts,
# data will also be kept
#spring.jpa.hibernate.ddl-auto=update

spring.jpa.generate-ddl=false

#hibernate config
spring.jpa.database-platform=org.hibernate.dialect.Oracle10gDialect
